<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title></title>
</head>
<body>
    {% if token.language == 'fr' %}
        {{ token.book.FollowUpEmailFr}}
    {% elseif token.language == 'es' %}
        {{ token.book.FollowUpEmailEs}}
    {% else %}
        {{ token.book.FollowUpEmailEt}}  
    {% endif %}
</body>
</html>