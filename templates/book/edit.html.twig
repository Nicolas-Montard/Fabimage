{% extends 'base.html.twig' %}

{% block title %}Edit Book{% endblock %}

{% block body %}
    <div class="book-form">
        <h1 class="text-center mb-4 text-primary">Mettre a jour le livre</h1>
        {% if error %}
            <p class="text-center"> vous devez remplir tous les champs</p>
        {% endif %}
        {{ include('book/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        <div class="d-flex justify-content-center mt-4">
            {{ include('book/_delete_form.html.twig') }}
        </div>
    <div class="d-flex justify-content-center mt-4">
        <a href="{{ path('app_promotional_code_new', {id: book.id}) }}" class=""><button class="btn btn-secondary text-white">
                Créer un code promotionnel
            </button></a>
        <a href="{{ path('app_promotional_code_index', {id: book.id}) }}" class="mx-3"><button class="btn btn-secondary text-white">
                Mettre a jour les codes promotionnels
            </button></a>
    </div>
    <div class="d-flex justify-content-center mt-4">
        <a href="{{ path('app_follow_up_email_new', {id: book.id}) }}" class=""><button class="btn btn-secondary text-white">
                Créer un Email de suivie
            </button></a>
        <a href="{{ path('app_follow_up_email_index', {id: book.id}) }}" class="mx-3"><button class="btn btn-secondary text-white">
                Mettre a jour les Emails de suivie
            </button></a>
    </div>
    </div>
{% endblock %}
