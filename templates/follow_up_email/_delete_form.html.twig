<form method="post" action="{{ path('app_follow_up_email_delete', {'id': follow_up_email.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ follow_up_email.id) }}">
    <div class=" d-flex justify-content-center mt-3">
        <button class="btn btn-secondary text-white">Supprimer</button>
    </div>
</form>
