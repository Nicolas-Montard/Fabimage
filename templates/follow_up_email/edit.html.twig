{% extends 'base.html.twig' %}

{% block title %}Edit FollowUpEmail{% endblock %}

{% block body %}
<div class="followUpEmail-form">
    <h1 class="text-primary text-center mb-3">Mettre a jour l'email'</h1>
    {% if error %}
    <p class="text-center"> vous devez remplir tous les champs</p>
    {% endif %}
    {{ include('follow_up_email/_form.html.twig', {'button_label': 'Mettre a jour'}) }}

    {{ include('follow_up_email/_delete_form.html.twig') }}
</div>
{% endblock %}
