{% extends 'base.html.twig' %}

{% block title %}Edit PromotionalCode{% endblock %}

{% block body %}
    <div class="promotionalCode-form">
        <h1 class="text-center text-primary mb-4">Mettre à jour le code promotionel</h1>

        <div class="d-flex justify-content-center">
            {{ include('promotional_code/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        </div>
        <div class="d-flex justify-content-center mt-3">
            {{ include('promotional_code/_delete_form.html.twig') }}
        </div>
        <div class="d-flex justify-content-center mt-3">
            <a href="{{ path('app_promotional_code_index', {'id': book.id}) }}">
                <button class="btn btn-secondary rounded text-white">Retour à la liste</button>
            </a>
        </div>
    </div>
{% endblock %}
