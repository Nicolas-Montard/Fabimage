<form method="post" action="{{ path('app_tariff_delete', {'id': tariff.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tariff.id) }}">
    <button class="btn">Delete</button>
</form>
