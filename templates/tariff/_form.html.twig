{{ form_start(form) }}
    <div class="d-flex justify-content-around">
        <div class="d-flex flex-column container-field">
            {{ form_label(form.titleFr, 'Titre français') }}
            {{ form_widget(form.titleFr) }}
            {{ form_errors(form.titleFr) }}
        </div>
        <div class="d-flex flex-column container-field">
            {{ form_label(form.titleEs, 'Titre espagnol') }}
            {{ form_widget(form.titleEs) }}
            {{ form_errors(form.titleEs) }}
        </div>
        <div class="d-flex flex-column container-field">
            {{ form_label(form.titleEt, 'Titre estonien') }}
            {{ form_widget(form.titleEt) }}
            {{ form_errors(form.titleEt) }}
        </div>
    </div>
    <div class="d-flex justify-content-center">
        <div class="d-flex flex-column mt-4">
            {{ form_label(form.pictureFile, 'Image de la prestation') }}
            {{ form_widget(form.pictureFile) }}
            {{ form_errors(form.pictureFile) }}
        </div>
    </div>
    <div class="d-flex justify-content-center">
        <div class="d-flex flex-column mt-4">
            {{ form_label(form.contentFr, 'Texte français') }}
            {{ form_widget(form.contentFr) }}
            {{ form_errors(form.contentFr) }}
        </div>
    </div>
    <div class="d-flex justify-content-center">
        <div class="d-flex flex-column mt-4">
            {{ form_label(form.contentEs, 'Texte espagnol') }}
            {{ form_widget(form.contentEs) }}
            {{ form_errors(form.contentEs) }}
        </div>
    </div>
    <div class="d-flex justify-content-center">
        <div class="d-flex flex-column mt-4">
            {{ form_label(form.contentEt, 'Texte estonien') }}
            {{ form_widget(form.contentEt) }}
            {{ form_errors(form.contentEt) }}
        </div>
    </div>
    <div class="d-flex justify-content-center">
        <button class="btn btn-secondary text-white mt-5 fs-5">{{ button_label|default('sauvegarder') }}</button>
    </div>
{{ form_end(form) }}
